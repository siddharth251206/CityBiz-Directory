<%- include('partials/header', { title: title, currentPage: 'business', stylesheets: ['businessDetail.css'] }) %>

<main class="detail-container">

    <section class="detail-header">
        <div class="header-image-gallery">
            <img src="<%= business.image || 'https://placehold.co/800x400/eee/ccc?text=No+Image' %>" alt="<%= business.name %>">
        </div>
        <div class="header-info">
            <span class="header-category"><%= business.category_name %></span>
            <h1><%= business.name %></h1>
            <div class="header-rating">
                <span class="rating-stars">⭐ <%= business.avg_rating %></span>
                <span class="review-count">(<%= business.review_count %> reviews)</span>
            </div>
            <div class="header-contact">
                <p>📞 <%= business.phone %></p>
                <p>📧 <%= business.email %></p>
                <p>📍 <%= business.address %>, <%= business.city %>, <%= business.state %> <%= business.pincode %></p>
            </div>
            <div class="header-actions">
                <a href="<%= business.website || '#' %>" class="btn-action" ${!business.website ? 'disabled' : ''}>🌐 Website</a>
                <button class="btn-action" id="favorite-btn">❤️ Add to Favorites</button>
            </div>
        </div>
    </section>

    <section class="detail-content">
        
        <div id="overview" class="detail-tab-content">
            <h2>Overview</h2>
            <p class="description"><%= business.description %></p>

            <h3>Operating Hours (Sample)</h3>
            <ul class="hours-list">
                <li><strong>Monday:</strong> 9:00 AM - 6:00 PM</li>
                <li><strong>Tuesday:</strong> 9:00 AM - 6:00 PM</li>
                <li><strong>Wednesday:</strong> 9:00 AM - 6:00 PM</li>
                <li><strong>Thursday:</strong> 9:00 AM - 6:00 PM</li>
                <li><strong>Friday:</strong> 9:00 AM - 8:00 PM</li>
                <li><strong>Saturday:</strong> 10:00 AM - 4:00 PM</li>
                <li><strong>Sunday:</strong> Closed</li>
            </ul>
        </div>

        <div class="review-form-container" 
             data-id="<%= business.business_id %>" 
             data-owner-id="<%= business.owner_id %>">
            
            <h3 id="leave-review-title">Leave a Review</h3>
            <h3 id="customer-reviews-title" style="display: none;">Reviews by Customers</h3>

            <form class="review-form">
                <div class="form-group">
                    <label for="rating">Rating (1-5):</label>
                    <select id="rating" name="rating" required>
                        <option value="" disabled selected>Select a rating</option>
                        <option value="5">5 - Excellent</option>
                        <option value="4">4 - Good</option>
                        <option value="3">3 - Average</option>
                        <option value="2">2 - Poor</option>
                        <option value="1">1 - Terrible</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="comment">Your review:</label>
                    <textarea id="comment" name="comment" rows="4" placeholder="Share your experience..."></textarea>
                </div>
                <button type="submit" class="btn-submit-review">Submit Review</button>
            </form>
        </div>

        <div class="detail-tab-content">
            <h2>All Reviews</h2>
            <div class="review-list" id="review-list-container">
                <% if (reviews.length > 0) { %>
                    <% reviews.forEach(review => { %>
                        <div class="review-card">
                            <div class="review-header">
                                <span class="review-author"><%= review.user_name %></span>
                                <span class="review-rating">⭐ <%= review.rating %>.0</span>
                            </div>
                            <p class="review-comment"><%= review.comment %></p>
                            <span class="review-date">
                                Posted on <%= new Date(review.created_at).toLocaleDateString() %>
                            </span>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p>No reviews yet. Be the first to leave one!</p>
                <% } %>
            </div>
        </div>
    </section>
</main>

<%- include('partials/footer', { scripts: ['businessDetail.js'] }) %>